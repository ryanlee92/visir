import 'package:flutter/widgets.dart';

class VisirBadge extends StatelessWidget {
  const VisirBadge({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
