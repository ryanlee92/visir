import 'package:flutter/widgets.dart';

class VisirEmptyState extends StatelessWidget {
  const VisirEmptyState({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
