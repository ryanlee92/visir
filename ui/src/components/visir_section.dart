import 'package:flutter/widgets.dart';

class VisirSection extends StatelessWidget {
  const VisirSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
