import 'package:flutter/widgets.dart';

class VisirInput extends StatelessWidget {
  const VisirInput({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
