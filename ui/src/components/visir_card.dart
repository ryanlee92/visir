import 'package:flutter/widgets.dart';

class VisirCard extends StatelessWidget {
  const VisirCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
