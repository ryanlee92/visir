import 'package:flutter/widgets.dart';

class VisirSpinner extends StatelessWidget {
  const VisirSpinner({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
