import 'package:flutter/widgets.dart';

class VisirIconButton extends StatelessWidget {
  const VisirIconButton({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
