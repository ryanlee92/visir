import 'package:flutter/widgets.dart';

class VisirButton extends StatelessWidget {
  const VisirButton({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
