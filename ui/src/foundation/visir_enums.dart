enum VisirButtonVariant { primary, secondary, ghost, danger }

enum VisirButtonSize { sm, md, lg }

enum VisirCardVariant { elevated, muted, outlined }

enum VisirCardDensity { compact, comfortable, spacious }

enum VisirBadgeTone { neutral, primary, success, warning, danger }

enum VisirSpinnerTone { neutral, primary, inverse }

enum VisirSpinnerSize { sm, md, lg }
